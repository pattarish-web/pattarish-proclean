# Agent notes — cleaning-seo-website

1. Prefer `sangkan-seo-content-safety` before touching `posts.json`, `seo.yml`, blog bots, or landings.
2. Prefer `sangkan-line-ops` before touching `ops/webhook`, LIFF, Sheets billing.
3. Prefer `sangkan-dual-repo` if unsure whether work belongs in `sangkan-clean`.

## Skills

See `.agents/skills/README.md` and `.cursor/skills/`.

## Machine migration

See `docs/MACHINE_SETUP.md` and `scripts/restore-agent-skills.ps1`.
